<% if user_signed_in? %>
<div class="hero-unit">
	<h1>My Groups</h1>
	<p>
		<%= link_to "Bachelor Party", "#", class: "btn btn-primary btn-large" %> 
		<%= link_to "Ski Trip", "#", class: "btn btn-primary btn-large" %> 
    <%= link_to "Family Vacation", "#", class: "btn btn-primary btn-large" %> 

	</p>
</div>
<% else %>
<div class="hero-unit">
	<h1>Welcome To HipTripster</h1>
	<p>
	      Start Planning Your Next Adventure NOW!
	</p>
	<p>
		<%= link_to "Sign Up Now!", new_user_registration_path, class: "btn btn-primary btn-large" %> 
	</p>

</div>
<% end %>